/*
**Codewars Daily Challenge Log**

- **Challenge:** https://www.codewars.com/kata/5287e858c6b5a9678200083c/javascript
- **Date Completed:** August 23, 2025
- **Difficulty:** 6 kyu
- **Profile:** https://www.codewars.com/users/andysantiagopr

**Solution Code (with comments):**
```javascript
function narcissistic(value) {
  
  let splitted = value.toString().split('').map(Number); 
  // Convert the input number into a string, split it into individual characters,
  // then map each character back to a number (e.g., 153 → [1, 5, 3]).

  let power = splitted.length; 
  // Store the total number of digits, which is the exponent for each digit.

  if (splitted.length === 1) { 
    // Any single-digit number is always narcissistic (e.g., 7).
    return true;
  }
  
  let result = splitted.reduce((acc, currentValue) => { 
    // Iterate through each digit, raising it to the power of the total digit count,
    // and accumulate the result into a sum.
    return acc + (currentValue ** power); 
  }, 0);
  
  return result === value; 
  // Compare the computed sum with the original number.
  // If they match, the number is narcissistic.
}
